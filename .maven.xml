<!-- settings.xml a destination des postes de developpement -->
<settings>
	<servers>
		<server>
			<id>ossrh</id>
			<username>${SONATYPE_USERNAME}</username>
			<password>${SONATYPE_PASSWORD}</password>
		</server>
	</servers>
	<mirrors>
	</mirrors>

	<profiles>
		<profile>
			<id>ossrh</id>
			<activation>
				<activeByDefault>true</activeByDefault>
			</activation>
			<properties>
				<gpg.executable>${GPG_EXECUTABLE}</gpg.executable>
				<gpg.passphrase>${GPG_PASSPHRASE}</gpg.passphrase>
				<gpg.keyname>${GPG_KEYNAME}</gpg.keyname>
				<gpg.defaultKeyring>false</gpg.defaultKeyring>
				<gpg.publicKeyring>${GPG_DIR}/private-key.gpg</gpg.publicKeyring>
				<gpg.secretKeyring>${GPG_DIR}/public-key.asc</gpg.secretKeyring>
			</properties>
		</profile>
	</profiles>

	<activeProfiles>
		<activeProfile>ossrh</activeProfile>
	</activeProfiles>

</settings>
